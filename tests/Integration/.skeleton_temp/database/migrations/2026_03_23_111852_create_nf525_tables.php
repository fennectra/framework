<?php

return [
    'up' => 'CREATE TABLE IF NOT EXISTS invoices ( id INTEGER PRIMARY KEY AUTOINCREMENT, number TEXT NOT NULL UNIQUE, client_name TEXT NOT NULL, client_address TEXT, client_siret TEXT, total_ht REAL NOT NULL DEFAULT 0, tva REAL NOT NULL DEFAULT 0, total_ttc REAL NOT NULL DEFAULT 0, is_credit INTEGER DEFAULT 0, credit_of INTEGER, credit_reason TEXT, hash TEXT NOT NULL DEFAULT \'\', previous_hash TEXT NOT NULL DEFAULT \'0\', created_at TEXT DEFAULT CURRENT_TIMESTAMP, updated_at TEXT DEFAULT CURRENT_TIMESTAMP); CREATE TABLE IF NOT EXISTS invoice_lines ( id INTEGER PRIMARY KEY AUTOINCREMENT, invoice_id INTEGER NOT NULL REFERENCES invoices(id), description TEXT NOT NULL, quantity REAL NOT NULL DEFAULT 1, unit_price REAL NOT NULL DEFAULT 0, tva_rate REAL NOT NULL DEFAULT 20, total_ht REAL NOT NULL DEFAULT 0, total_ttc REAL NOT NULL DEFAULT 0, created_at TEXT DEFAULT CURRENT_TIMESTAMP); CREATE TABLE IF NOT EXISTS nf525_closings ( id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL, period_start TEXT NOT NULL, period_end TEXT NOT NULL, total_ht REAL NOT NULL DEFAULT 0, total_tva REAL NOT NULL DEFAULT 0, total_ttc REAL NOT NULL DEFAULT 0, cumulative_total REAL NOT NULL DEFAULT 0, document_count INTEGER NOT NULL DEFAULT 0, hash TEXT NOT NULL, previous_hash TEXT NOT NULL DEFAULT \'0\', created_at TEXT DEFAULT CURRENT_TIMESTAMP, UNIQUE(type, period_start, period_end)); CREATE TABLE IF NOT EXISTS nf525_journal ( id INTEGER PRIMARY KEY AUTOINCREMENT, event TEXT NOT NULL, entity_type TEXT, entity_id INTEGER, details TEXT DEFAULT \'{}\', user_id INTEGER, ip_address TEXT, created_at TEXT DEFAULT CURRENT_TIMESTAMP)',
    'down' => 'DROP TABLE IF EXISTS nf525_journal; DROP TABLE IF EXISTS nf525_closings; DROP TABLE IF EXISTS invoice_lines; DROP TABLE IF EXISTS invoices',
];

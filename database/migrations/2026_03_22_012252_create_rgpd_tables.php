<?php

return [
    'up' => 'CREATE TABLE IF NOT EXISTS consent_objects ( id SERIAL PRIMARY KEY, object_name VARCHAR(255) NOT NULL, object_content TEXT NOT NULL, object_version INTEGER NOT NULL DEFAULT 1, object_previous_version INTEGER DEFAULT NULL REFERENCES consent_objects(id), key VARCHAR(50) DEFAULT NULL, is_required BOOLEAN DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE INDEX IF NOT EXISTS idx_consent_objects_key ON consent_objects (key); CREATE INDEX IF NOT EXISTS idx_consent_objects_version ON consent_objects (key, object_version); CREATE TABLE IF NOT EXISTS user_consents ( id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE, consent_object_id INTEGER NOT NULL REFERENCES consent_objects(id) ON DELETE CASCADE, consent_status BOOLEAN NOT NULL DEFAULT FALSE, consent_way VARCHAR(50) NOT NULL DEFAULT \'web\', object_version INTEGER NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE INDEX IF NOT EXISTS idx_user_consents_user ON user_consents (user_id); CREATE INDEX IF NOT EXISTS idx_user_consents_object ON user_consents (consent_object_id); CREATE UNIQUE INDEX IF NOT EXISTS idx_user_consents_unique ON user_consents (user_id, consent_object_id)',
    'down' => 'DROP TABLE IF EXISTS user_consents; DROP TABLE IF EXISTS consent_objects',
];
